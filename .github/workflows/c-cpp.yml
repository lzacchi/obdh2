name: C/C++ CI

on:
  push:
    branches: [ github_actions ]
  pull_request:
    branches: [ github_actions ]

jobs:
  build:

    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./firmware
    
    steps:
    - uses: actions/checkout@v2
    - name: cmocka
      run: sudo apt-get install libcmocka0 libcmocka-dev
    - name: make current_sensor
      run: cd tests/devices && echo "DID CD"
    - name: current_sensor
      run: cd ../ && echo "DID OTHER CD"  
    - name: clean
      run: cd ../ && echo "DID THIRD CD"
